package site.metacoding.ex13;


import java.awt.Checkbox;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.awt.event.ItemEvent;
import java.awt.event.ItemListener;

import javax.swing.JButton;
import javax.swing.JCheckBox;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JPanel;

public class EventEx01 extends JFrame {

	JPanel myPanel;
	JButton btn1;
	JButton btn2;
	JCheckBox checkBox1;
	JLabel labelText, labelIMG;

	public EventEx01() {
		setSize(300, 300);
		setLocationRelativeTo(null); // 프레임 화면 중앙 배치
		setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // x버튼 클릭시 main 종료

		myPanel = new JPanel();
		add(myPanel);

		checkBox1 = new JCheckBox();
		myPanel.add(checkBox1);
		checkBox1.addItemListener(new ItemListener() {

			@Override
			public void itemStateChanged(ItemEvent e) {
				System.out.println(e.toString()); //이거는 내부의 상태를 보여주는 문법
				System.out.println(e.getStateChange()); // 이거는 내부의 변화를 보여주는 문법 

				if (e.getStateChange() == 1) {
					System.out.println("체크되었습니다");
				} else {
					System.out.println("체크 해제되었습니다");
				}
			}
		});

		btn1 = new JButton("버튼1을 클릭하세요");
		myPanel.add(btn1);

		btn2 = new JButton("버튼2를 클릭하세요");
		myPanel.add(btn2);
		// 윈도우야 지켜봐줘 이 버튼을!!
		btn1.addActionListener(new ActionListener() {

			// 버튼이 클릭되면 윈도우가 해당 메서드를 콜백해준다.
			@Override
			public void actionPerformed(ActionEvent e) {
				System.out.println("버튼이 클릭되었습니다");
			}
		});

		btn2.addActionListener((ActionEvent e) -> {
			System.out.println("버튼2가 클릭되었습니다.");

		});

		setVisible(true);

	}

	private void initSetting() {}
	private void initObect() {}	
	private void initListner() {}
	public static void main(String[] args) {

		new EventEx01();

	}
}